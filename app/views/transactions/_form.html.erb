
<div class='container rounded text-light font-weight-bold'>
  <div class="form-group">

      <%= form_with(model: transaction) do |form| %>
    <% if transaction.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(transaction.errors.count, "error") %> prohibited this transaction from being saved:</h2>

        <ul>
          <% transaction.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>


    <div class="field">
      <%=form.label :select_group %>   
      <%
=begin%>
 <%=select("transaction", "group_id", Group.all.collect {|g|[g.name, g.id] }, { include_blank: true })%> 
<%
=end%>
      <%= f.select :group_id, Group.all.collect(:transaction),:id,:name, include_blank: true %>
    </div>

    <div class="field">
      <%= form.label :name  %>
      <%= form.text_field :name %>
    </div>

    <div class="field">
      <%= form.label :amount%>
      <%= form.number_field :amount%>
    </div>

    <div class="actions">
      <%= form.submit %>
    </div>
  <% end %>
 </div>
</div>
